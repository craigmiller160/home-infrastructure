ingress_test:
  name: ingress-test
ingress:
  name: cluster-ingress
  hosts:
    - name: dev-craigmiller160.ddns.net
      services:
        - path: /ingress-test
          serviceName: "{{ .Values.ingress_test.name }}"
          portNumber: 80